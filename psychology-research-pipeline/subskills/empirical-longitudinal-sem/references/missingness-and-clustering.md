# 缺失与聚类

先报告各波缺失、单调/非单调模式、流失预测和关键变量缺失差异。主模型使用冻结的 FIML 或插补策略；若 FIML 为主，`mice` 的插补模型必须包含分析变量、流失预测变量和群组结构，并报告插补数、迭代、诊断与合并规则。

学校或班级聚类已知时，报告聚类数、规模分布与 ICC。聚类数有限时优先使用有小样本修正的稳健协方差；多层模型作为结构敏感性。学校固定效应只能控制已观察学校差异，不能替代所有聚类不确定性处理。
