# 纵向 SEM 报告契约

每个模型保存：分析编号、分类、输入数据哈希、规格哈希、代码哈希、R/包版本、随机种子、样本和各波有效人数、估计量、缺失处理、聚类处理、收敛、警告、拟合、参数、标准误、置信区间、标准化方式与偏离。

主要路径表必须标明个体内/个体间层级、时间间隔、约束、参照组和多重性策略。性别差异报告直接约束检验或对比及不确定性。稳健性表同时保留支持、削弱与矛盾结果，不按显著性筛选。

只有独立核验角色确认哈希、收敛、异常估计和正文数字后，写作角色才能读取该结果。
